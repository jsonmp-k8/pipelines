# Do NOT commit real credentials. Create the secret manually:
#
#   kubectl create secret generic ai-api-key \
#     --namespace=kubeflow \
#     --from-literal=ANTHROPIC_API_KEY=<your-key>
#
# Or use a sealed-secret / external-secrets-operator in production.
